/**
 * StickyColumnControls — PR 3 (stub)
 *
 * Implement in PR 3 branch.
 *
 * Will render:
 *  - ToggleControl for stickyFirstColumn
 *
 * The getSaveElement filter (with-save-element.js) already contains the scroll
 * container wrapper logic for this feature — it just needs the toggle wired up.
 *
 * Remember to handle the box-shadow border simulation for the sticky column
 * (border-collapse: collapse breaks position: sticky — see PLAN.md).
 */

export default function StickyColumnControls() {
	return null;
}
